import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.SpectralGT.FluidSimulation',
  appName: 'Fluid Simulation',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
